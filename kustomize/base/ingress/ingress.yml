apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: default
spec:
  ingressClassName: "nginx"
  rules:
  - host: todo.yogdev.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: app-1-service
            port: 
                number: 8000     
